<article class="task-widgets">
	<TaskProgress @tasks={{model}}/>

	<TaskTimeline @tasks={{model}} @onAddTask={{this.addTask}}/>
</article>

<DashboardWidget @title="Inbox" @count={{this.messages.length}}>
	<MessageList @messages={{this.messages}}/>
</DashboardWidget>

{{outlet}}